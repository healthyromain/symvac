<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand ms-2" href="{{ path('home.index') }}">Navbar</a>

    <div class="navbar-nav">
        <a class="nav-link active" href="{{ path('home.index') }}">Home</a>
    </div>
</nav>